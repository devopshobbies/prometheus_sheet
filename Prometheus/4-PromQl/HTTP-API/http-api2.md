

**مانیتورینگ زیرساخت شما نباید یک کار بی‌هدف باشد.** API پرومتئوس به شما کمک می‌کند تا متریک‌های درستی را استخراج کنید و واقعاً بدانید چه خبر است. چه در ابتدای راه باشید و چه بخواهید از تنظیمات فعلی خود سر در بیاورید، این راهنما نحوه استفاده از API را برای رسیدن به پاسخ‌های مورد نیازتان – بدون حدس و گمان – توضیح می‌دهد.

‏**API پرومتئوس: کلید درک متریک‌های شما**
‏API پرومتئوس فقط ابزار دیگری نیست که به مجموعه ابزارهای فنی پر و پیمان شما اضافه شود؛ بلکه یک عنصر مخفی است که به شما امکان دسترسی مستقیم به تمام متریک‌های ارزشمندی که پرومتئوس جمع‌آوری می‌کند را می‌دهد.

به زبان ساده، API پرومتئوس روشی است که شما با سرور پرومتئوس خود صحبت می‌کنید. آن را مانند یک "نگهبان" تصور کنید که از بخش VIP متریک‌ها محافظت می‌کند. این API به شما اجازه می‌دهد تا داده‌هایی را که پرومتئوس از سیستم‌های شما جمع‌آوری می‌کند، کوئری‌، تحلیل و استخراج کنید.

**چرا این موضوع برای شما اهمیت دارد؟**
زیرا با فراخوانی‌های صحیح API، می‌توانید:
* دقیقاً متریک‌هایی را که نیاز دارید، بدون غرق شدن در داده‌های اضافه، استخراج کنید.
* داده‌های پرومتئوس را با ابزارها و داشبوردهای دیگر خود ادغام کنید.
* پاسخ‌های خودکار به شرایط خاص متریک‌ها را تنظیم کنید.
* راه‌حل‌های مانیتورینگ سفارشی بسازید که دقیقاً متناسب با زیرساخت شما باشد.

‏API پرومتئوس در دو نوع اصلی ارائه می‌شود: HTTP API برای کوئری‌های مستقیم و Management API برای مدیریت خود پرومتئوس. بر هر دو مسلط شوید، و شما عملاً همان "ابرقهرمان مانیتورینگ" خواهید بود که تیمتان نمی‌دانست به آن نیاز دارد.

💡 با توابع کلیدی پرومتئوس آشنا شوید و نحوه استفاده مؤثر از آن‌ها برای کوئری‌ و تحلیل متریک‌ها را بیاموزید: **بیشتر بخوانید.**

**دسترسی به متریک‌های شما با API پرومتئوس**
بیایید با اصول اولیه شروع کنیم. API پرومتئوس با پروتکل HTTP کار می‌کند، به این معنی که می‌توانید با استفاده از ابزارهایی که قبلاً می‌شناسید، مانند cURL یا Postman، با آن کار کنید.

نقطه ورود به "گنجینه داده" پرومتئوس معمولاً در آدرس زیر در دسترس است:
`http://your-prometheus-server:9090/api/v1/`

بیایید نقاط پایانی (endpoints) ضروری را که روزانه از آن‌ها استفاده خواهید کرد، بررسی کنیم:


| نقطه پایانی            | کاربرد                                                      | زمان استفاده                                                |
| ---------------------- | ----------------------------------------------------------- | ----------------------------------------------------------- |
| `/query`               | کوئری‌های لحظه‌ای را اجرا می‌کند                            | زمانی که به مقدار فعلی یک متریک‌ نیاز دارید                 |
| `/query_range`         | کوئری‌های بازه‌ای را اجرا می‌کند                            | زمانی که به داده‌ها در طول یک دوره زمانی نیاز دارید         |
| `/series`              | سری‌های زمانی منطبق با مجموعه‌ای از برچسب‌ها را پیدا می‌کند | هنگام کاوش در مورد وجود متریک‌ها                            |
| `/labels`              | تمام نام برچسب‌ها را دریافت می‌کند                          | زمانی که نیاز دارید بدانید چه ابعادی (dimensions) وجود دارد |
| `/label/<name>/values` | مقادیر یک برچسب خاص را دریافت می‌کند                        | هنگام فیلتر کردن بر اساس یک بعد خاص                         |

در اینجا یک نمونه سریع برای شروع کار آورده شده است – دریافت مصرف فعلی CPU با cURL:

`curl 'http://your-prometheus-server:9090/api/v1/query?query=sum(rate(node_cpu_seconds_total{mode!="idle"}[1m])) by (instance)'`

```sh
curl -G http:/0.0.0.0:9090/api/v1/query \
  --data-urlencode 'query=sum(rate(node_cpu_seconds_total{mode!="idle"}[10m])) by (instance)'
```

این دستور یک درخواست HTTP به نقطه پایانی query سرور پرومتئوس شما ارسال می‌کند. این درخواست، مجموع نرخ مصرف CPU (به استثنای زمان بیکاری) را در دقیقه گذشته، گروه‌بندی شده بر اساس نمونه (instance)، می‌خواهد. نتیجه یک نمای کلی سریع از اینکه کدام سرورها در حال حاضر بیشتر کار می‌کنند را به شما می‌دهد.

این دستور نرخ مصرف CPU را در تمام نمونه‌های شما استخراج می‌کند. پاسخ به صورت JSON برمی‌گردد – ساختاریافته، تمیز و آماده برای تجزیه و تحلیل.

💡 انواع مختلف متریک‌های پرومتئوس و نحوه استفاده مؤثر از آن‌ها را درک کنید: **[بیشتر بخوانید.](https://last9.io/blog/prometheus-metrics-types-a-deep-dive/)**

**نحوه کوئری‌ دقیق متریک‌های شما**
قدرت واقعی API پرومتئوس در PromQL – زبان کوئری‌ پرومتئوس – نهفته است. این زبان مانند SQL برای متریک‌های شماست، اما با قابلیت‌های فوق‌العاده سری‌های زمانی.

‏PromQL پایه از طریق API ساده است:

`curl 'http://your-prometheus-server:9090/api/v1/query?query=up'`

این کوئری‌ی ساده، متریک‌ "up" را برمی‌گرداند که پرومتئوس از آن برای ردیابی آنلاین (1) یا آفلاین (0) بودن اهداف استفاده می‌کند. این اساساً یک بررسی سلامت است که به شما نشان می‌دهد کدام سیستم‌ها توسط پرومتئوس با موفقیت مانیتورینگتورینگ می‌شوند و کدام یک ممکن است مشکلات اتصال داشته باشند.

این کوئری‌ نشان می‌دهد که کدام اهداف فعال (1) یا غیرفعال (0) هستند. ساده، اما فوق‌العاده مفید زمانی که می‌خواهید بفهمید چرا هشدارهای شما ساعت 3 صبح به صدا در می‌آیند.

بیایید به چیزی کاربردی‌تر برویم – پیدا کردن سرورهایی که فضای دیسک کمی دارند:

`curl 'http://your-prometheus-server:9090/api/v1/query?query=node_filesystem_avail_bytes/node_filesystem_size_bytes*100 < 20'`


```sh
​￼curl -G http:/0.0.0.0:9090/api/v1/query \
  --data-urlencode 'query=node_filesystem_avail_bytes/node_filesystem_size_bytes*100 < 20'
```

این دستور سیستم هشدار اولیه شما برای مشکلات فضای دیسک است. این دستور درصد فضای خالی در هر سیستم فایل را محاسبه می‌کند و نمونه‌هایی را که در آن‌ها کمتر از 20% است، فیلتر می‌کند. نتیجه لیستی از سیستم‌هایی است که به سطوح خطرناک استفاده از فضای ذخیره‌سازی نزدیک می‌شوند و به شما زمان می‌دهد تا قبل از از کار افتادن برنامه‌ها، فضا را پاکسازی یا ظرفیت را افزایش دهید.

این کوئری‌ نمونه‌هایی را برمی‌گرداند که فضای دیسک در دسترس کمتر از 20٪ است – به شما زمان می‌دهد تا قبل از اینکه سرورهایتان دچار مشکل شوند، به این مسئله رسیدگی کنید.

💡 نحوه نوشتن کوئری‌های PromQL را بیاموزید که واقعاً به شما در درک سیستمتان کمک می‌کنند: **بیشتر بخوانید.**

**تکنیک‌های پیشرفته پرومتئوس که تیم شما به آن‌ها تکیه خواهد کرد**
حالا که اصول اولیه را می‌دانید، بیایید به برخی از حرکات پیشرفته بپردازیم که تازه‌کاران مانیتورینگ را از حرفه‌ای‌ها جدا می‌کند.

**اتوماسیون پایتون:**
چرا وقتی می‌توانید خودکار کنید، متریک‌ها را به صورت دستی بررسی کنید؟ در اینجا یک قطعه کد پایتون برای دریافت و پردازش متریک‌های پرومتئوس آورده شده است:

```python
import requests
import json

def query_prometheus(query):
    url = 'http://0.0.0.0:9090/api/v1/query'
    response = requests.get(url, params={"query": query})
    results = response.json()['data']['result']
    return results

# Get the 5 nodes with highest memory usage
high_mem_nodes = query_prometheus('topk(5, node_memory_MemAvailable_bytes/node_memory_MemTotal_bytes)')


for node in high_mem_nodes:
    instance = node['metric']['instance']
    usage = float(node['value'][1]) * 100
    print(f"{instance}: {usage:.2f}% memory is free")

```
این اسکریپت پایتون یک تابع قابل استفاده مجدد برای کوئری‌ از سرور پرومتئوس شما ایجاد می‌کند. اسکریپت ابتدا یک تابع کمکی تعریف می‌کند که درخواست HTTP و تجزیه JSON را مدیریت می‌کند، سپس از آن برای یافتن 5 گره (node) با بالاترین مصرف حافظه در زیرساخت شما استفاده می‌کند.

برای هر گره، نام نمونه (instance) را استخراج کرده و درصد مصرف حافظه را محاسبه می‌کند و یک خروجی تمیز و فرمت‌بندی شده نمایش می‌دهد. این برای بررسی‌های منظم یا به عنوان بخشی از یک اسکریپت مانیتورینگ بزرگتر که می‌تواند هنگام تغییر الگوهای مصرف حافظه هشدار ارسال کند، عالی است.

این اسکریپت را به صورت زمان‌بندی شده اجرا کنید، و همیشه خواهید دانست که کدام سرورها قبل از اینکه به مشکل تبدیل شوند، حافظه زیادی مصرف می‌کنند.

#### **هشداردهی سفارشی (DIY Alerting):**
در حالی که پرومتئوس قوانین هشداردهی مخصوص به خود را دارد، گاهی اوقات به چیزی پویاتر نیاز دارید. می‌توانید از API کوئری‌ کرده و منطق سفارشی را پیاده‌سازی کنید:

```python
def check_custom_condition():
    # Query for error rate across services
    error_rates = query_prometheus('sum(rate(http_requests_total{status=~"5.."}[5m])) by (service) / sum(rate(http_requests_total[5m])) by (service) > 0.05')
    
    if error_rates:
        # More than 5% error rate detected
        affected_services = [rate['metric']['service'] for rate in error_rates]
        send_slack_alert(f"High error rates detected in: {', '.join(affected_services)}")
```
این تابع یک سیستم هشداردهی هوشمندتر از آنچه هشدارهای استاندارد پرومتئوس می‌توانند ارائه دهند، پیاده‌سازی می‌کند. این تابع نرخ خطا را برای هر سرویس با تقسیم نرخ خطاهای 5xx بر نرخ کل درخواست‌ها محاسبه می‌کند.

هنگامی که هر سرویس از نرخ خطای 5% فراتر رود، نام تمام سرویس‌های متاثر را جمع‌آوری کرده و یک هشدار Slack یکپارچه ارسال می‌کند. این رویکرد خستگی از هشدار را با گروه‌بندی مسائل مرتبط کاهش می‌دهد و تنها زمانی هشدار می‌دهد که نرخ خطا از یک آستانه معنی‌دار عبور کند، نه صرفاً زمانی که تعداد خطاهای خام افزایش می‌یابد.

این مثال به شما هشدار می‌دهد زمانی که هر سرویس نرخ خطایی بالای 5% داشته باشد – چیزی که ممکن است در هشدارهای سنتی مبتنی بر آستانه، نادیده گرفته شود.

💡 نکات و استراتژی‌های عملی برای مقیاس‌پذیری کارآمد پرومتئوس را بیاموزید: **بیشتر بخوانید.**

### **کشف متریک‌ها (Metric Discovery):**
‏API پرومتئوس فقط برای کوئری‌ از متریک‌ها نیست – می‌تواند به شما کمک کند تا بفهمید چه متریک‌هایی در دسترس هستند و چه معنایی دارند:

`# Get all metric names`
`curl 'http://your-prometheus-server:9090/api/v1/labels'`

`# Get all values for the 'job' label`
`curl 'http://your-prometheus-server:9090/api/v1/label/job/values'`

`# Get metadata about metrics`
`curl 'http://your-prometheus-server:9090/api/v1/metadata'`

این دستورات به شما کمک می‌کنند تا کاوش کنید چه متریک‌هایی در سیستم پرومتئوس شما وجود دارد. اولین کوئری‌ تمام نام برچسب‌های موجود را برمی‌گرداند، که به شما می‌گوید بر اساس چه ابعادی می‌توانید فیلتر و گروه‌بندی کنید.

کوئری‌ی دوم تمام مقادیر برای برچسب 'job' را لیست می‌کند و هر job مانیتورینگ‌شده در زیرساخت شما را نشان می‌دهد. کوئری‌ی نهایی فراداده (metadata) مربوط به تمام متریک‌ها، از جمله نوع آن‌ها (counter, gauge, histogram) و متن کمکی را برمی‌گرداند.

این کاوش فراداده زمانی که با سیستمی جدید کار می‌کنید یا در تلاش برای یافتن متریک‌های مناسب برای یک نیاز مانیتورینگ خاص هستید، بسیار ارزشمند است.

**نحوه اتصال API پرومتئوس با ابزارهای دیگر**
‏API پرومتئوس قرار نیست به صورت جداگانه کار کند – ارزش واقعی آن از اتصال آن با ابزارهای دیگر شما حاصل می‌شود.

### **ادغام Grafana**
بله، Grafana از قبل منبع داده پرومتئوس را دارد، اما گاهی اوقات به کنترل بیشتری نیاز دارید. می‌توانید از Grafana HTTP API با Prometheus API برای ایجاد داشبوردهای پویا استفاده کنید:

```bash
# Create a new Grafana dashboard with Prometheus API data
curl -X POST -H "Content-Type: application/json" -d '{
  "dashboard": {
    "title": "Dynamic API Dashboard",
    "panels": [
      {
        "title": "Custom Error Rates",
        "type": "graph",
        "datasource": "Prometheus",
        "targets": [
          {
            "expr": "sum(rate(http_requests_total{status=~\"5..\"}[5m])) by (service) / sum(rate(http_requests_total[5m])) by (service) > 0.01",
            "refId": "A"
          }
        ]
      }
    ]
  },
  "overwrite": true
}' http://grafana:3000/api/dashboards/db
```
این دستور از Grafana HTTP API برای ایجاد برنامه‌نویسی یک داشبورد استفاده می‌کند که داده‌ها را از پرومتئوس نمایش می‌دهد.

یک داشبورد جدید با عنوان "Dynamic API Dashboard" با یک پنل نمودار واحد ایجاد می‌کند که سرویس‌ها با نرخ خطای بالای 1% را نشان می‌دهد.

پارامتر `overwrite` تضمین می‌کند که اگر داشبوردی با همین نام قبلاً وجود داشته باشد، به جای ایجاد یک نسخه تکراری، به روز رسانی می‌شود. این رویکرد به شما امکان می‌دهد داشبوردهای خود را به عنوان کد مدیریت کرده و به طور خودکار به عنوان بخشی از تنظیمات زیرساخت خود استقرار دهید.

این یک داشبورد ایجاد می‌کند که سرویس‌ها با نرخ خطای بالای 1% را مانیتورینگ می‌کند – به شما دید دقیقی در جایی که به آن نیاز دارید، می‌دهد.

**ادغام با خط لوله CI/CD**
استقرار خود را با ادغام بررسی‌های Prometheus API در خط لوله CI/CD خود هوشمندتر کنید:

```bash
#!/bin/bash
# Script to verify service health post-deployment

# Deploy the service
deploy_service

# Wait for service to initialize
sleep 30

# Query error rate for the first 5 minutes after deployment
error_rate=$(curl -s 'http://prometheus:9090/api/v1/query' \
  --data-urlencode 'query=sum(rate(http_requests_total{service="newly-deployed",status=~"5.."}[5m])) / sum(rate(http_requests_total{service="newly-deployed"}[5m])) or vector(0)' \
  | jq -r '.data.result[0].value[1]')

# If error rate exceeds threshold, rollback
if (( $(echo "$error_rate > 0.05" | bc -l) )); then
  echo "New deployment showing high error rate: $error_rate. Rolling back."
  rollback_deployment
  exit 1
else
  echo "Deployment successful! Error rate: $error_rate"
fi
```
این اسکریپت شل، مانیتورینگ پرومتئوس را مستقیماً در خط لوله استقرار شما ادغام می‌کند و یک شبکه ایمنی برای انتشار شما ایجاد می‌کند. پس از استقرار یک سرویس، 30 ثانیه برای شروع به کار منتظر می‌ماند، سپس از پرومتئوس نرخ خطای سرویس تازه مستقر شده را کوئری‌ می‌کند.

این کوئری‌ نسبت خطاهای 5xx به کل درخواست‌ها را محاسبه می‌کند، با یک مقدار بازگشتی به صفر در صورتی که هنوز داده‌ای وجود نداشته باشد. اگر نرخ خطا از 5% فراتر رود، اسکریپت به طور خودکار یک بازگشت به عقب را آغاز کرده و با کد خطا خارج می‌شود و از تأثیر استقرار بد بر کاربران جلوگیری می‌کند. قسمت `or vector(0)` به طور خاص هوشمندانه است و تضمین می‌کند که کوئری‌ حتی اگر سرویس هنوز هیچ درخواستی دریافت نکرده باشد، یک نتیجه معتبر برگرداند.

💡 با نحوه کار Prometheus Remote Write و زمان استفاده از آن برای ذخیره‌سازی طولانی‌مدت آشنا شوید: **[بیشتر بخوانید](https://last9.io/blog/what-is-prometheus-remote-write/).**

### **اجتناب از مشکلات رایج API**
حتی باتجربه‌ترین مهندسان DevOps نیز با API پرومتئوس دچار مشکل می‌شوند. در اینجا به برخی از اشتباهات رایج اشاره شده است که باید از آنها اجتناب کنید:

**مشکلات مهلت زمانی (Timeout Troubles): وقتی کوئری‌ها برای همیشه طول می‌کشند**
کوئری‌های پیچیده در بازه‌های زمانی بزرگ می‌توانند سریع‌تر از سرد شدن قهوه صبحگاهی شما، به مهلت زمانی برسند. راه‌حل؟ آن‌ها را به قطعات کوچک‌تر تقسیم کنید:

```bash
# Instead of this massive query
curl 'http://prometheus:9090/api/v1/query_range?query=sum(rate(http_requests_total[5m]))&start=1609459200&end=1625097600&step=1h'

# Split it into smaller chunks
for start in {1609459200..1625097600..86400}; do
  end=$((start + 86400))
  curl "http://prometheus:9090/api/v1/query_range?query=sum(rate(http_requests_total[5m]))&start=$start&end=$end&step=1h"
  sleep 1
done
```
این اسکریپت یک رویکرد هوشمندانه برای مدیریت بازه‌های زمانی بزرگ در کوئری‌های پرومتئوس را نشان می‌دهد. به جای درخواست شش ماه داده در یک کوئری‌ی واحد (که احتمالاً به مهلت زمانی می‌رسد)، درخواست را به قطعات روزانه قابل مدیریت تقسیم می‌کند. اسکریپت کل بازه زمانی را در افزونه‌های 86,400 ثانیه (یک روز) تکرار می‌کند و برای هر روز یک فراخوانی API جداگانه انجام می‌دهد. یک ثانیه مکث بین درخواست‌ها از غرق شدن سرور پرومتئوس شما جلوگیری می‌کند. این رویکرد به ویژه برای تولید گزارش‌ها یا تحلیل روندهای طولانی‌مدت بدون مواجه شدن با مشکلات مهلت زمانی مفید است.

##### بررسی `rate()` در مقابل `increase()` در PromQL 
توابع `rate()` و `increase()` در PromQL اغلب هنگام کوئری‌ از طریق API افراد را گیج می‌کنند. در اینجا راهنمای سریع آورده شده است:

* از `rate()` برای میانگین‌های هر ثانیه استفاده کنید (عالی برای سنجش عملکرد فعلی).
* از `increase()` برای افزایش‌های کلی در طول زمان استفاده کنید (ایده‌آل برای صورت‌حساب یا گزارش‌های استفاده).

```bash
# Per-second rate of requests
curl 'http://prometheus:9090/api/v1/query?query=rate(http_requests_total[5m])'

# Total number of requests in the last hour
curl 'http://prometheus:9090/api/v1/query?query=increase(http_requests_total[1h])'
```
این دو دستور تفاوت اصلی بین `rate()` و `increase()` در PromQL را نشان می‌دهند. کوئری‌ی اول نرخ درخواست‌ها را در هر ثانیه در یک بازه 5 دقیقه‌ای محاسبه می‌کند و توان عملیاتی فعلی سیستم شما را نشان می‌دهد. این برای داشبوردهای زمان واقعی یا متریک‌های استفاده ایده‌آل است.

کوئری‌ی دوم کل افزایش در شمارنده را در ساعت گذشته محاسبه می‌کند و تعداد مطلق درخواست‌های پردازش شده را نشان می‌دهد. این برای صورت‌حساب، گزارش‌های استفاده، یا برنامه‌ریزی ظرفیت که به تعداد کلی نیاز دارید تا نرخ‌ها، بهتر است.

### **بحران کاردینالیته بالا (High Cardinality Crisis):**
متریک‌های با کاردینالیته بالا می‌توانند سرور پرومتئوس شما را کندتر از صبح دوشنبه کنند. هنگام استفاده از API، به تعداد سری‌های زمانی که کوئری‌های شما ایجاد می‌کنند، توجه داشته باشید:

```bash
# Bad: Creates a time series for every possible status code and path combination
curl 'http://prometheus:9090/api/v1/query?query=sum(rate(http_requests_total[5m])) by (status_code, path)'

# Better: Group by service and status code range
curl 'http://9090/api/v1/query?query=sum(rate(http_requests_total{status=~"(2|3|4|5).."}[5m])) by (service, status)'
```
این مثال‌ها نشان می‌دهند که چگونه از انفجار کاردینالیته در پرومتئوس جلوگیری کنید. کوئری‌ی اول متریک‌ها را بر اساس کد وضعیت و مسیر گروه‌بندی می‌کند، که می‌تواند هزاران یا حتی میلیون‌ها سری زمانی ایجاد کند اگر برنامه شما URLهای پویا زیادی داشته باشد.

نسخه بهبود یافته از یک regex برای تطبیق با بازه‌های کد وضعیت (مانند 2xx، 3xx) استفاده می‌کند و بر اساس سرویس به جای مسیرهای جداگانه گروه‌بندی می‌کند. این به طور چشمگیری تعداد سری‌های زمانی را کاهش می‌دهد در حالی که همچنان اطلاعات قابل اقدامی در مورد اینکه کدام سرویس‌ها دچار خطا هستند، ارائه می‌دهد.

نکته اصلی: هنگام طراحی کوئری‌ها، به ویژه برای داشبوردهای پرکاربرد، به برچسب‌های با کاردینالیته بالا توجه داشته باشید.

💡 اگر با مشکلات پرومتئوس مواجه هستید، نحوه رفع چالش‌های کاردینالیته، مصرف منابع و ذخیره‌سازی را بیاموزید: **[بیشتر بخوانید!](https://last9.io/blog/troubleshooting-common-prometheus-pitfalls-cardinality-resource-utilization-and-storage-challenges/)**

### **نحوه ساخت ابزارهای سفارشی با API پرومتئوس**
چرا به مانیتورینگ‌های آماده بسنده کنید وقتی می‌توانید راه‌حل‌های سفارشی بسازید؟ در اینجا چند ایده برای تحریک خلاقیت شما آورده شده است:

### **مانیتورینگ SLO سفارشی (Custom SLO Monitoring)**
اهداف سطح سرویس (SLO) حیاتی هستند، اما ردیابی آن‌ها می‌تواند مشکل‌ساز باشد. این اسکریپت پایتون یک ردیاب SLO سفارشی ایجاد می‌کند:

```python
import requests
import time
from datetime import datetime, timedelta

def check_error_budget():
    # Calculate error rate over the last 30 days
    end_time = int(time.time())
    start_time = end_time - (30 * 24 * 60 * 60)  # 30 days in seconds
    
    query = 'sum(rate(http_requests_total{status=~"5.."}[1d])) / sum(rate(http_requests_total[1d]))'
    
    url = f'http://prometheus:9090/api/v1/query_range'
    params = {
        'query': query,
        'start': start_time,
        'end': end_time,
        'step': '1d'  # Daily samples
    }
    
    response = requests.get(url, params=params).json()
    values = response['data']['result'][0]['values']
    
    # Calculate average error rate
    error_rates = [float(v[1]) for v in values]
    avg_error_rate = sum(error_rates) / len(error_rates)
    
    # 99.9% availability target means 0.1% error budget
    error_budget = 0.001
    budget_remaining = error_budget - avg_error_rate
    budget_percent = (budget_remaining / error_budget) * 100
    
    return {
        'average_error_rate': avg_error_rate,
        'error_budget_remaining': budget_remaining,
        'error_budget_percent_remaining': budget_percent
    }

result = check_error_budget()
print(f"SLO Status: {result['error_budget_percent_remaining']:.2f}% of error budget remaining")
```
اسکریپت بالا یک سیستم مانیتورینگ SLO را با استفاده از API پرومتئوس پیاده‌سازی می‌کند و:

* نرخ خطا را در 30 روز گذشته با تحلیل نسبت خطاهای 5xx به کل درخواست‌ها، با نمونه‌برداری روزانه، محاسبه می‌کند.
* نرخ خطای میانگین را با یک بودجه خطای از پیش تعریف شده (0.1%، همراستا با 99.9% دسترسی) مقایسه می‌کند.
* خروجی‌ها:
  * نرخ خطای میانگین در طول دوره.
  * بودجه خطای باقیمانده مطلق.
  * درصد بودجه باقیمانده.
* به تیم‌ها کمک می‌کند تا تصمیم بگیرند چه زمانی اولویت را به قابلیت اطمینان بدهند یا توسعه ویژگی.
* اگر بیشتر بودجه خطا مصرف شده باشد، نشان‌دهنده نیاز به تمرکز بر پایداری قبل از راه‌اندازی ویژگی‌های جدید است. برای ارزیابی اینکه آیا یک استقرار پرخطر در محدوده قابلیت اطمینان قابل قبول است، مفید است.

### **سیستم تشخیص ناهنجاری (Anomaly Detection System)**
اگر می‌خواهید قبل از اینکه مشکلات به حادثه تبدیل شوند، از آن‌ها پیشی بگیرید، این اسکریپت ساده تشخیص ناهنجاری از API پرومتئوس برای شناسایی الگوهای غیرعادی استفاده می‌کند:

```python
# Assuming 'detect_anomalies' function is defined elsewhere
# (e.g., using numpy for statistical calculations as hinted by the original text)

def detect_anomalies(query, threshold=3): # Simplified for example
    url = 'http://your-prometheus-server:9090/api/v1/query_range'
    params = {
        'query': query,
        'start': int(time.time()) - 86400, # Last 24 hours
        'end': int(time.time()),
        'step': '5m'
    }

    response = requests.get(url, params=params).json()

    anomalies = []

    if 'data' in response and 'result' in response['data']:
        for result in response['data']['result']:
            metric = result['metric']
            values = [float(v[1]) for v in result['values']]
            
            # Calculate Z-scores
            # (Requires numpy, assuming it's available in the actual implementation)
            try:
                import numpy as np
                mean = np.mean(values)
                std = np.std(values)
                
                if std == 0:  # Skip flat metrics
                    continue
                    
                z_scores = [(v - mean) / std for v in values]
                
                # Find anomalies (Z-score exceeds threshold)
                for i, z in enumerate(z_scores):
                    if abs(z) > threshold:
                        timestamp = result['values'][i][0]
                        value = result['values'][i][1]
                        anomalies.append({
                            'metric': metric,
                            'timestamp': timestamp,
                            'value': value,
                            'z_score': z
                        })
            except ImportError:
                print("NumPy not found. Cannot calculate Z-scores for anomaly detection.")
                break # Exit if numpy is not available for this functionality

    return anomalies

# Detect unusual CPU spikes
cpu_anomalies = detect_anomalies('node_cpu_seconds_total{mode="system"}')

for anomaly in cpu_anomalies:
    print(
        f"Anomaly detected on {anomaly['metric']['instance']} "
        f"at {datetime.fromtimestamp(anomaly['timestamp'])}"
    )
```
این اسکریپت مقادیر غیرعادی متریک‌ها را با استفاده از Z-score شناسایی می‌کند – ایده‌آل برای تشخیص آن فرآیند سرکش که CPU را مصرف می‌کند یا آن افزایش ناگهانی در نرخ خطا.

💡 اگر مطمئن نیستید که تابع `rate()` پرومتئوس چگونه کار می‌کند، این راهنما آن را با مثال توضیح می‌دهد: **بیشتر بخوانید.**

**آینده API پرومتئوس**
اکوسیستم پرومتئوس در حال تکامل است و API نیز از این قاعده مستثنی نیست. در اینجا چیزهایی است که باید به آن‌ها توجه کنید:

‏* **Exemplars:** نسخه‌های جدیدتر پرومتئوس از exemplars پشتیبانی می‌کنند – ردیابی‌های خاصی که به شما کمک می‌کنند متریک‌ها را با ردیابی‌ها مرتبط کنید.
* **بهبودهای Remote Write API:** ارسال متریک‌های پرومتئوس به ذخیره‌سازی طولانی‌مدت را آسان‌تر می‌کند.
* **بهبودهای Federation:** راه‌های بهتر برای کوئری‌ در چندین سرور پرومتئوس.

**گام‌های بعدی شما با API پرومتئوس**
اگر تازه با مانیتورینگ آشنا شده‌اید یا به دنبال فراتر رفتن از مرزهای آنچه با پرومتئوس ممکن است هستید، API راه شما به سمت "بهشت مانیتورینگ" است.

**به یاد داشته باشید:**
* با کوئری‌های ساده شروع کنید.
* وظایف مانیتورینگ تکراری را خودکار کنید.
* با ابزارهای موجود خود ادغام شوید.
* راه‌حل‌های سفارشی برای نیازهای خاص خود بسازید.

💡 به انجمن دیسکورد ما بپیوندید تا در مورد چالش‌های روزمره توسعه‌دهندگان بحث کنید، دیدگاه‌ها را به اشتراک بگذارید و با دیگرانی که علایق مشترکی دارند ارتباط برقرار کنید.

**سوالات متداول (FAQs)**
**تفاوت بین Prometheus HTTP API و Management API چیست؟**
‏HTTP API همان چیزی است که بیشتر اوقات از آن استفاده خواهید کرد – برای کوئری‌ از متریک‌ها و فراداده (metadata) است. Management API برای مدیریت خود پرومتئوس است، از جمله بارگذاری مجدد پیکربندی و گرفتن عکس‌های لحظه‌ای (snapshots). HTTP API را به عنوان نحوه صحبت شما با متریک‌های خود و Management API را به عنوان نحوه صحبت شما با خود پرومتئوس در نظر بگیرید.

**آیا می‌توانم از Prometheus API برای ذخیره‌سازی طولانی‌مدت استفاده کنم؟**
در حالی که می‌توانید داده‌های تاریخی را از طریق API کوئری‌ کنید، خود پرومتئوس برای ذخیره‌سازی طولانی‌مدت طراحی نشده است. برای این منظور، باید از قابلیت "remote write" استفاده کنید تا متریک‌ها را به سیستم‌هایی مانند Thanos، Cortex یا راه‌حل‌های ارائه‌دهندگان ابری که می‌توانند سال‌ها داده‌های متریک‌ را ذخیره کنند، ارسال کنید.

**چگونه Prometheus API را امن کنم؟**
به طور پیش‌فرض، Prometheus API شامل احراز هویت نیست. برای محیط‌های تولیدی، باید پرومتئوس را پشت یک پروکسی معکوس مانند Nginx قرار دهید یا از پیکربندی داخلی TLS و احراز هویت پایه (basic auth) پرومتئوس استفاده کنید. رویکرد دیگر استفاده از OAuth2 Proxy به عنوان یک کانتینر sidecar در صورت اجرای در Kubernetes است.

**بهترین راه برای مدیریت محدودیت‌های نرخ (rate limits) با API چیست؟**
پرومتئوس محدودیت نرخ داخلی برای API خود ندارد، اما می‌تواند با کوئری‌های زیاد، دچار مشکل شود. محدودیت نرخ سمت کلاینت را در برنامه‌های خود پیاده‌سازی کنید، کش کردن کوئری‌های مکرر را در نظر بگیرید، و از federation برای توزیع بار کوئری‌ در چندین نمونه پرومتئوس استفاده کنید.

**چگونه کوئری‌های کند API را اشکال‌زدایی کنم؟**
‏`/debug/pprof` را به URL پرومتئوس خود اضافه کنید تا به نقاط پایانی پروفایلینگ دسترسی پیدا کنید. برای عملکرد کوئری‌ به طور خاص، پارامتر `stats=1` را به کوئری‌ی خود اضافه کنید تا اطلاعات دقیقی در مورد مدت زمان هر مرحله به دست آورید. به عنوان مثال: `http://prometheus:9090/api/v1/query?query=up&stats=1`

**آیا می‌توانم از Prometheus API با زبان‌های برنامه‌نویسی دیگر استفاده کنم؟**
کاملاً! در حالی که ما مثال‌های پایتون را نشان دادیم، کتابخانه‌های کلاینت رسمی و جامعه‌محور برای اکثر زبان‌های محبوب از جمله Go، Java، Ruby و Node.js وجود دارند. همه آن‌ها الگوهای مشابهی برای کوئری‌ و تجزیه نتایج دنبال می‌کنند.

**چگونه احراز هویت را هنگام کوئری‌ از API مدیریت کنم؟**
اگر نمونه پرومتئوس شما پشت احراز هویت پایه (basic authentication) قرار دارد، می‌توانید اعتبارنامه‌ها را در درخواست‌های API خود قرار دهید. برای احراز هویت مبتنی بر توکن، یک هدر `Authorization` با توکن خود اضافه کنید.




-----------
### سایر

 فاکتور **Z-score** (اسکور Z) یک معیار آماری است که نشان می‌دهد **یک مقدار چقدر از میانگین دور است** و این دوری را بر حسب **انحراف معیار** بیان می‌کند.

---

# ✅ تعریف دقیق

Z-score به صورت زیر تعریف می‌شود:


$Z = \frac{x - \mu}{\sigma}$


که در آن:

* **x**: مقدار داده
* **μ**: میانگین (Mean)
* **σ**: انحراف معیار (Standard Deviation)

---

# ✅ مفهوم

Z-score به شما می‌گوید:

* **Z = 0** → مقدار دقیقا برابر میانگین است
* **Z = +1** → مقدار یک انحراف معیار بالاتر از میانگین است
* **Z = −2** → مقدار دو انحراف معیار پایین‌تر از میانگین است

---

# 📌 مثال ساده

فرض کنید قد افراد یک کلاس:

* میانگین: 170 cm
* انحراف معیار: 10 cm
* قد یک نفر: 190 cm

Z-score:



$Z = \frac{190 - 170}{10} = 2$



یعنی این نفر **۲ انحراف معیار بالاتر از میانگین** است.

---

# 🎯 کاربردها

* تشخیص داده‌های **غیرعادی / Outlier**
* نرمال‌سازی داده‌ها (Standardization)
* تحلیل آماری در Machine Learning و AI
* مقایسه متغیرهایی با واحدهای مختلف
* محاسبه احتمال در توزیع نرمال

---

اگر خواستی، می‌تونم:

* یک مثال واقعی با داده‌های شما حساب کنم
* Z-score را روی یک فایل CSV اجرا کنم
* توضیح بدم چطور برای **تشخیص نقاط غیرعادی** استفاده می‌شود.
